# Targets: quality (bugs + reliability + security + DX) — 2026-07-15
repo: /Users/michael/forge/plugins/iris @ 25066a5 (+ uncommitted npm publish-prep on packages/{core,opencode,mcp}/package.json + bun.lock + rebuilt opencode dist) | formula: impact(J) × opportunity(J)
swept: all first-party sources under packages/{core,opencode,mcp,skill} + scripts/ (~14 sources, ~7.3k LOC) | mode: **refresh** of quality-2026-07-09 + residual-2026-07-10; small-repo direct scoring (no Haiku fan-out)
history caveat: only **16 commits** total / <90d — churn ranks are low-confidence; scoring is judgment-led with live doctor + code evidence
live probe 2026-07-15: `iris doctor` VERDICT no action needed; Runtime files all CURRENT; hostConnected=true; broker pid 6394 (PPID 1 since Jul 12)
prior fixed (do not re-open): press/CDP keys, doctor STALE, default-tab active preference, network JSON body scrub (v1), plane labels + IRIS_FALLBACK=off, agent fail-loud, CDP click, skill inventory CI, 4.8.0 git release

Scores are impact(1–5) × opportunity(1–5). Discard threshold: either factor ≤ 2.

## 1. Snapshot returns live input values including passwords — 20 (impact 5 × opportunity 4) [open]
Files: `packages/core/extension/background.js` `toolSnapshot` build (~L2019–2023)
Evidence:
- For every visible `INPUT`/`TEXTAREA`, snapshot sets `node.value = el.value` with **no** `type === "password"` gate and no secret masking
- Snapshot is a high-frequency agent tool; values land in model context, tool logs, and transcripts
- Password fields, session tokens pasted into inputs, OTP boxes, and filled credit-card fields all serialize
- Adjacent: `toolQuery` can still read values via `mode=property` (intentional escape hatch) — snapshot should not dump them by default
Handoff:
> Fix ONE scored target: stop leaking sensitive field values from browser_snapshot in packages/core/extension/background.js. Mask password/hidden secret inputs (omit or `[redacted]`); optionally omit empty values; keep non-secret input values if needed for form filling or gate behind an explicit `includeValues` flag defaulting false. Acceptance: snapshot of a page with `<input type=password value=secret>` never contains `secret`; normal buttons/links still work. One target only; report adjacent query/network leaks for re-scoring.

## 2. Network redaction still incomplete for non-JSON bodies + some secret headers — 16 (impact 4 × opportunity 4) [open]
Files: `packages/core/extension/background.js` `redactHeaders` (~L358–377), `redactNetworkBody` (~L379–417), `toolNetworkGet` (~L2460–2469)
Evidence:
- Body scrub is **JSON-key-only**; `application/x-www-form-urlencoded`, raw JWT lines, XML, and multipart pass through unchanged when `includeBody=true`
- `base64Encoded: true` bodies skip scrub entirely (`redacted: false`) — common for compressed/binary-ish CDP payloads that may still be UTF-8 JSON underneath
- Headers: `*token*` / `*api-key*` / cookie / authorization covered; **`x-amz-credential`**, **`x-session-id`**, generic `credential` / `session` names **PASS**
- `network_get` always serializes headers via `serializeNetworkRecord(record, true)` even when body omitted
Handoff:
> Fix ONE scored target: harden network redaction. (1) Expand header denylist (credential/session/signature families). (2) For bodies: attempt base64 decode when flagged; scrub form-urlencoded keys with the same secret regex; keep JSON deep scrub. (3) Prefer headers redacted on get always. Acceptance: form body `password=x&access_token=y` and header `x-amz-credential: …` never leave the tool verbatim when includeBody/includeHeaders paths run; unit-cover pure redact helpers without Chrome. One target only.

## 3. Snapshot `uid` is not an action handle (selector/ref gap) — 16 (impact 4 × opportunity 4) [open]
Files: `packages/core/extension/background.js` `toolSnapshot` (~L2007–2030); `pageOps` locators (~L1041–1052); `packages/opencode/src/plugin.ts` `browser_click`/`browser_type` (selector-only); agent snapshot returns `refs` (`agent-backend.ts` ~L820–827)
Evidence:
- Extension snapshot emits `uid: "eN"` plus a **lossy** derived CSS (`#id` or `tag.cls1.cls2`) that collides / drifts
- Click/type/press resolve CSS / label / aria / text / role / name / placeholder / id — **no `uid` / `ref` / `eN` locator**
- Agents that follow Playwright-style “snapshot then click e12” fail or click the wrong element
- Agent-backend plane returns a different `refs` map; plane switch changes addressing model mid-session
Handoff:
> Fix ONE scored target: make snapshot uids actionable. Prefer: (a) accept `selector: "uid=e12"` or `ref=e12` in resolveLocator and keep a per-tab weakmap from last snapshot, or (b) always emit a stable unique selector (data attribute stamped at snapshot time). Acceptance: snapshot → click by returned uid hits the same element without hand-written CSS. Do not break existing CSS/label locators. One target only.

## 4. v4.8.0 tag / npm publish prep divergence — 16 (impact 4 × opportunity 4) [open]
Files: `packages/opencode/package.json`, `packages/mcp/package.json`, `packages/core/package.json`, `bun.lock`; tag `v4.8.0` @ `25066a5`
Evidence:
- Tag still has `"@mizner/iris": "workspace:*"` (runtime) and `"@mizner/iris-opencode": "file:../opencode"` — npm consumers cannot resolve
- Working tree (this session) moves bundled deps to `devDependencies` + `workspace:*`, adds `files`/`publishConfig`/`prepublishOnly` — **correct for bun publish**, uncommitted
- Publishing tarballs from the dirty tree without landing them means **npm 4.8.0 ≠ git tag 4.8.0**
- Skill: `@mizner/iris-skill` is `private: true` (correct); monorepo root private (correct)
Handoff:
> Fix ONE scored target: land publish-prep on a branch/PR, keep versions 4.8.0 **or** cut 4.8.1 if you refuse to rewrite the tag story. Acceptance: `bun pm pack` in each of core/opencode/mcp produces registry-safe package.json (no `workspace:*`/`file:` in published dependencies); git tree that matches what will be published is on main via PR; document publish order core → opencode → mcp. Do not npm publish from this machine without auth. One target only.

## 5. No unit harness for extension pure logic (redact / locators / snapshot) — 12 (impact 3 × opportunity 4) [open]
Files: `packages/core/test/` (broker-only); `packages/core/extension/background.js` pure helpers mixed into 2.5k line IIFE-ish module
Evidence:
- `bun run test` = 8 broker integration tests + skill-tool string check — **zero** coverage of `redactHeaders` / `redactNetworkBody` / locator parse / snapshot masking
- Prior residual pass unit-tested redaction only via ad-hoc parent smoke, not committed tests
- Regression risk on #1/#2 fixes without extractable pure modules
Handoff:
> Fix ONE scored target: extract (or export-under-test) pure redaction + locator helpers and add node:test coverage. Acceptance: `bun run test` fails if password snapshot masking or form-body redaction regresses. Do not stand up a full Chrome extension runner in this target. One target only.

## Candidates (unscored / next sweep)
- Empty `profileEmails` = allow-all — **documented intentional** (README + skill); threat-model SECURITY.md still missing
- Agent-backend still missing wait_for/network/console/errors/highlight/claims (~10 tools) — fail-loud now; parity is product work, not silent failure
- `IRIS_BACKEND=agent` skips extension plane when fallbacks on — intentional primary mode; skill under-documents it
- Header `x-session-id` / non-token session cookies in **bodies** that are not JSON
- Broker auto-claims first unowned active tab for a session (by design after #3); multi-agent lease UX still rough
- MCP process guards lack uncaughtException handler (orphan risk low; stdin/ppid covered)
- Template skill vs packages/skill still diverge on prose; inventory CI only checks tool names in packages/skill
- Snapshot stamps no `data-iris-uid` — pairs with #3
- CWS/build-cws-package path not re-validated this sweep

## Resolved (carry-forward)
- #1 keyboard / browser_press (2026-07-09)
- #2 doctor runtime drift CURRENT/STALE (2026-07-09)
- #3 default tab prefers active Chrome tab (2026-07-09)
- network JSON body scrub v1 + plane labels + IRIS_FALLBACK=off + agent fail-loud + CDP click + skill inventory CI (2026-07-10)
- 4.8.0 git release/tag/PR #2 (2026-07-10); npm still blocked on auth + target #4

## Gate
Do **not** dispatch root-cause-fixer / Fable until a named target is approved in chat.
Recommended next: **#1 snapshot secrets** (security, small) → **#2 network redaction** → **#3 uid actions** → **#4 publish land** (process, before any npm publish) → **#5 unit harness** (supports #1/#2).
