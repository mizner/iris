# Targets: quality (bugs + reliability + security + DX) — 2026-07-09
repo: /Users/michael/forge/plugins/iris @ ffe49ff (+3 uncommitted follow-ups: tool-test import, pong-freshness test, badge broker-source gate) | formula: impact(J) × opportunity(J)
swept: all first-party sources under packages/{core,opencode,mcp,skill} + bin/ + docs/ (~15 files, ~7k LOC) | mode: small-repo direct scoring (no Haiku fan-out; 11 commits / ~37 days so churn ranks are low-confidence)
prior list: .sisyphus/targets/quality-2026-07-07.md @ 4a3e8d8 — targets 1–6 fixed, #7 still open, #8 refuted
live probe 2026-07-09: doctor VERDICT ok; hostConnected=true; broker pid 91591 (PPID 1 since Jul 7); native-host pid 53307 healthy; ~/.iris/extension/background.js **stale vs repo** (badge fix not installed)

Scores are impact(1–5) × opportunity(1–5). Discard threshold: either factor ≤ 2.

## 1. No real keyboard input path (type sets value only; no press tool) — 20 (impact 5 × opportunity 4) [fixed]
Files: `packages/core/extension/background.js` pageOps `type` (~L1242–1280), `clickElement` (~L1048–1068); adapter surface `packages/opencode/src/plugin.ts` (browser_type only); agent backend `packages/opencode/src/agent-backend.ts` type case (~L739–759)
Evidence:
- type path: focus + `setNativeValue` + `input`/`change` Events only — zero `KeyboardEvent` (keydown/keypress/keyup)
- no `press` / `browser_press` tool in extension `executeTool` map or OpenCode/MCP tool list
- click uses synthetic `MouseEvent` + `el.click()` → `isTrusted:false`; many React/hotkey/Enter-to-submit UIs ignore this
- prior target #7 from 2026-07-07 still open; CDP Input.dispatchKeyEvent / dispatchMouseEvent already available once debugger attached (screenshot/network path)
Handoff:
> Fix ONE scored target: real keyboard (+ preferably trusted input) in packages/core/extension/background.js and wire adapters. Prefer chrome.debugger Input.dispatchKeyEvent / dispatchMouseEvent when debugger is attachable; keep DOM fallback. Add browser_press (Enter/Tab/Escape/arrows + modifiers). Acceptance: Enter submits a keydown-only form; autocomplete/hotkey UIs respond; type still works for plain controlled inputs. One target only; report adjacent findings for re-scoring.

## 2. Installed extension can silently lag repo (doctor blind to drift) — 16 (impact 4 × opportunity 4) [fixed]
Files: `packages/core/bin/cli.js` `update()` / `doctor()` / `status()`; runtime dir `~/.iris/extension`
Evidence:
- Live: `~/.iris/extension/background.js` ≠ repo `packages/core/extension/background.js` (badge gate missing; 2374 vs 2381 lines; different shasum)
- broker/native-host copies currently match line counts, but broker process started Jul 7 and never restarted after later installs
- doctor reports file presence + hostConnected only — no content hash / mtime / version compare
- reliability.md deployment checklist says run `cli.js update` then reconnect, but nothing enforces or detects skipped steps
Handoff:
> Fix ONE scored target: detect runtime drift in doctor/status. Hash (or mtime+size) compare packages/core/extension + installed ~/.iris/{extension,broker.cjs,native-host.cjs,browser-cli.cjs}; print STALE/CURRENT and a one-line “run iris update”. Optionally auto-offer update. Acceptance: after editing repo background.js without update, `iris doctor` flags extension STALE; after update, CURRENT. Do not change production defaults of ping/pong.

## 3. First wantsTab tool with no session default silently opens a new tab — 16 (impact 4 × opportunity 4) [fixed]
Files: `packages/core/bin/broker.cjs` `wantsTab` (L66–68), `handleTool` (L295–340), `ensureSessionTab` (L285–293)
Evidence:
- `wantsTab` excludes only get_tabs/get_active_tab/open_tab/list_downloads
- click/type/navigate/screenshot/query/etc. with no tabId and no defaultTabId call `ensureSessionTab` → `open_tab({active:false})`
- agents commonly omit tabId and expect the user’s active Chrome tab; instead they get a blank background tab and claim that
- get_active_tab returns the real active tab but does not set session default unless another path claims it
Handoff:
> Fix ONE scored target: default-tab selection policy in broker handleTool. When tabId missing, prefer (in order): session default claim → currently active Chrome tab (via get_active_tab + claim) → create only if no usable tab. Acceptance: browser_click without tabId operates on the user’s frontmost tab; no surprise blank tab. Keep multi-session claim isolation.

## 4. Agent-backend tool parity hole + silent router fallback — 12 (impact 3 × opportunity 4) [open]
Files: `packages/opencode/src/agent-backend.ts` `requestTool` switch (L636–855); `packages/opencode/src/plugin.ts` `routerRequest` (L354–399)
Evidence:
- Agent backend lacks: wait_for, highlight, console, errors, network_*, get_webmcp_status, get_profile_status, press (n/a), claim ops
- On extension failure, router falls through to AppleScript (tab ops only) then agent backend — models see success from a different plane without an explicit backend switch
- IRIS_BACKEND=agent users lose network/console/debugger features with hard “Unsupported tool” errors
- health plane reports three backends but tools don’t declare capability groups
Handoff:
> Fix ONE scored target: either (a) implement missing high-value agent tools or (b) make router/tool descriptions fail closed with capability metadata instead of opaque multi-plane fallback. Acceptance: when extension is down, user-visible error names which plane failed and which tools remain; agent backend documents supported subset; no silent plane switch on click/type.

## 5. network_get body path can leak secrets; redaction is header-only — 12 (impact 4 × opportunity 3) [open]
Files: `packages/core/extension/background.js` `redactHeaders` (L358–377), `toolNetworkGet` (L2255–2293)
Evidence:
- Headers redact authorization/cookie/set-cookie/*token*/*api-key* — good for list/get headers
- includeBody=true returns raw response body (base64 flag preserved) with only length truncation — tokens in JSON bodies pass through
- Skill warns agents not to log secrets, but the tool itself has no content-type gate, no body redaction, no default deny for auth-like URLs
- network_list defaults includeHeaders=false (safe); network_get always serializes headers when includeBody path builds `serializeNetworkRecord(record, true)` — headers always present on get
Handoff:
> Fix ONE scored target: harden network_get. Keep headers redacted; for bodies either (1) require explicit includeBody and strip obvious bearer/jwt/password fields in JSON, or (2) default includeBody off with stronger skill/tool description + size/type guards. Acceptance: sample OAuth token JSON response is not returned verbatim without an explicit unsafe flag; headers stay redacted.

## 6. Duplicate skill surfaces can drift — 9 (impact 3 × opportunity 3) [open]
Files: `packages/skill/SKILL.md` (iris product skill), `packages/core/templates/skills/browser-automation/SKILL.md` (install template), `.opencode/skills/browser-automation/SKILL.md`
Evidence:
- shasums differ: packages/skill vs core template are not the same product copy (140 vs 149 lines) — intentional split, but install path copies core template only
- `.opencode/skills/...` matches core template today; packages/skill is the Claude-facing iris skill and can diverge on tool lists (press/network wording)
- Not the consolidation banned in the prior plan, but still a discoverability/docs debt when tools change
Handoff:
> Fix ONE scored target: single source of tool inventory for skill docs (generate or shared fragment), or a check script that fails CI when skill tool lists omit executeTool/plugin tools. Acceptance: adding browser_press forces skill docs update or CI red.

## Candidates (unscored / next sweep)
- CDP trusted-click path superseding synthetic MouseEvent (pairs with fixed #1)
- Snapshot ref/uid addressing (Playwright-style) for click/type
- Profile allowlist empty = allow-all is intentional; document threat model in SECURITY.md
- Broker PPID 1 long-lived process: optional launchd recipe already in reliability.md — verify machine has it
- MCP process guards exist (stdin end + PPID→1); live orphans not observed (parents are claude/omp/grok)
- Extension unit-test harness still absent beyond broker tests
- Agent-backend still missing wait_for/network/console (open #4)
- network_get body redaction (open #5)
- Skill inventory drift check (open #6) — press docs updated manually this pass

## Resolved
- #1 keyboard / browser_press — CDP Input.dispatchKeyEvent + DOM KeyboardEvent fallback; type emits key events; plugin/MCP/agent wired; tests/docs updated (2026-07-09)
- #2 doctor runtime drift — sha256 compare of extension + broker/native-host/browser-cli; STALE forces non-green doctor verdict (2026-07-09)
- #3 default tab prefers active Chrome tab before open_tab; broker tests cover claimable + foreign-owned paths (2026-07-09)
- QA follow-ups: tool-test `@mizner/iris-opencode` import; pong-freshness isolation test; badge greens only on broker-sourced messages (2026-07-09)
- Broker multi-host registry + orphan fix — live hostConnected true; suite 8/8
- Driven keepalive pings — lastPongAgoMs healthy in doctor
- iris doctor / reconnect / live status — present and working
- Claude Code MCP path — documented
- Skill description boundary vs claude-in-chrome — packages/skill/SKILL.md states real-profile triggers

## Gate
Do **not** dispatch root-cause-fixer / Fable until a named target is approved in chat.
Next open ranked: **#4** (agent parity / silent fallback), **#5** (network_get body secrets), **#6** (skill inventory check).
